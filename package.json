{
  "name": "mostlane-po",
  "private": true,
  "description": "Mostlane PO system — Cloudflare Worker",
  "//": "type=module so the .js worker entry point is treated as ESM (it uses `export default`)",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "check": "node --check po-worker.js"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
